.round
  .order= round.order
  .groups-container
    = render round.groups
  .vertical-actions
    .vertical-action= link_to image_tag('plus.png', size: 12), new_game_round_group_path(round.game, round)
    .vertical-action= link_to image_tag('x.png', size: 12), round, method: :delete, data: CONF
%br