%h2.text-center Picking Random Wild Card...

.chosen-wc-name-container
  .chosen-wc-name= @rand_wc.name

%hr

.text-center
  - @wildcards.in_groups_of(6).each do |group|
    - group.each do |wc|
      - if wc == @rand_wc
        .chosen-wc= image_tag 'wild-card.png', width: 32, height: 32
      - else
        = image_tag 'wild-card.png', width: 32, height: 32
    %br
